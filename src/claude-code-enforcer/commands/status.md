# Airlock: Status

When the user invokes **/airlock:status**, show enforcement configuration.

## What to do

1. **Run**:
   ```bash
   ~/.config/airlock-enforcer/bin/airlock-enforcer status
   ```

2. **Summarize**: Report sign-in status, gateway URL, pairing, auto-mode, fail mode, and auto-approve pattern count.
